% f(x) = e^x - 3x = 0;

% xLow = 0; xHi = 1;

% yLow = expFunc(xLow);
% yHi = expFunc(xHi);

%   if yLow * yHi < 0 
%      xNew = (xLow + xHi)/2;
%      error = ((xNew - 0)/xNew) * 100;
%   else
%       end

% while error > 0.005
%   yNew = expFunc(xNew);
%   
%   if yLow * yHi < 0

%   if yNew > 0
%       xLow = xNew;
%       xOld = xNew;
%       xNew = (xLow + xHi)/2;
%       error = ((xNew - xOld)/xNew) * 100;
%   end
%   
%   yLow = expFunc(xLow);
%   yHi = expFunc(xHi);

%   if yLow * yHi < 0
%      xNew = (xLow + xHi)/2;
%      error = ((xNew - xOld)/xNew) * 100;